# Force Railway to use Node.js, not Gradle
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Skipping build step'"]

[start]
cmd = "npx tsx server/index.ts"