@import url('https://fonts.googleapis.com/css2?family=League+Spartan:wght@100;200;300;400;500;600;700;800;900&family=Quicksand:wght@300;400;500;600;700&family=Work+Sans:wght@100;200;300;400;500;600;700;800;900&display=swap');

@custom-variant dark (&:is(.dark *));

:root {
  --font-size: 14px;
  --pastel-yellow: #fef7cd;
  --pastel-blue: #a8dadc;
  --pastel-blue-dark: #89c4c7;
  --background: var(--pastel-yellow);
  --foreground: #1d1d1b;
  --card: #ffffff;
  --card-foreground: #1d1d1b;
  --popover: #ffffff;
  --popover-foreground: #1d1d1b;
  --primary: var(--pastel-blue);
  --primary-foreground: #1d1d1b;
  --secondary: #f8f9fa;
  --secondary-foreground: #1d1d1b;
  --muted: #e9ecef;
  --muted-foreground: #6c757d;
  --accent: var(--pastel-blue);
  --accent-foreground: #1d1d1b;
  --destructive: #dc3545;
  --destructive-foreground: #ffffff;
  --border: rgba(0, 0, 0, 0.1);
  --input: transparent;
  --input-background: #ffffff;
  --switch-background: #cbced4;
  --font-weight-medium: 500;
  --font-weight-normal: 400;
  --ring: var(--pastel-blue);
  --chart-1: #ff6b6b;
  --chart-2: #4ecdc4;
  --chart-3: #45b7d1;
  --chart-4: #f9ca24;
  --chart-5: #f0932b;
  --radius: 1rem;
  --sidebar: var(--pastel-yellow);
  --sidebar-foreground: #1d1d1b;
  --sidebar-primary: var(--pastel-blue);
  --sidebar-primary-foreground: #1d1d1b;
  --sidebar-accent: #ffffff;
  --sidebar-accent-foreground: #1d1d1b;
  --sidebar-border: rgba(0, 0, 0, 0.1);
  --sidebar-ring: var(--pastel-blue);
}

.dark {
  --background: #1d1d1b;
  --foreground: #f8f9fa;
  --card: #2d2d2b;
  --card-foreground: #f8f9fa;
  --popover: #2d2d2b;
  --popover-foreground: #f8f9fa;
  --primary: var(--pastel-blue);
  --primary-foreground: #1d1d1b;
  --secondary: #3d3d3b;
  --secondary-foreground: #f8f9fa;
  --muted: #4d4d4b;
  --muted-foreground: #adb5bd;
  --accent: var(--pastel-blue);
  --accent-foreground: #1d1d1b;
  --destructive: #dc3545;
  --destructive-foreground: #ffffff;
  --border: rgba(255, 255, 255, 0.1);
  --input: rgba(255, 255, 255, 0.1);
  --ring: var(--pastel-blue);
  --sidebar: #2d2d2b;
  --sidebar-foreground: #f8f9fa;
  --sidebar-primary: var(--pastel-blue);
  --sidebar-primary-foreground: #1d1d1b;
  --sidebar-accent: #3d3d3b;
  --sidebar-accent-foreground: #f8f9fa;
  --sidebar-border: rgba(255, 255, 255, 0.1);
  --sidebar-ring: var(--pastel-blue);
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-input-background: var(--input-background);
  --color-switch-background: var(--switch-background);
  --color-ring: var(--ring);
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius) - 2px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);
  --color-sidebar: var(--sidebar);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
}

@layer base {
  * {
    @apply border-border outline-ring/50;
  }

  body {
    @apply bg-background text-foreground;
    font-family: 'Quicksand', sans-serif;
  }
}

/**
 * Base typography. This is not applied to elements which have an ancestor with a Tailwind text class.
 */
@layer base {
  :where(:not(:has([class*=" text-"]), :not(:has([class^="text-"])))) {
    h1 {
      font-size: 2rem;
      font-weight: 600;
      line-height: 1.2;
      font-family: 'League Spartan', sans-serif;
    }

    h2 {
      font-size: 1.5rem;
      font-weight: 600;
      line-height: 1.3;
      font-family: 'League Spartan', sans-serif;
    }

    h3 {
      font-size: 1.25rem;
      font-weight: 500;
      line-height: 1.4;
      font-family: 'Work Sans', sans-serif;
    }

    h4 {
      font-size: 1rem;
      font-weight: 500;
      line-height: 1.5;
      font-family: 'Work Sans', sans-serif;
    }

    p {
      font-size: 1rem;
      font-weight: 400;
      line-height: 1.6;
      font-family: 'Quicksand', sans-serif;
    }

    label {
      font-size: 0.875rem;
      font-weight: 500;
      line-height: 1.5;
      font-family: 'Work Sans', sans-serif;
    }

    button {
      font-size: 1rem;
      font-weight: 500;
      line-height: 1.5;
      font-family: 'Work Sans', sans-serif;
    }

    input {
      font-size: 1rem;
      font-weight: 400;
      line-height: 1.5;
      font-family: 'Quicksand', sans-serif;
    }
  }
}

html {
  font-size: var(--font-size);
}

/* Brand spacing system - Tighter for mobile */
:root {
  --spacing-xs: 0.125rem; /* 2px */
  --spacing-sm: 0.25rem;  /* 4px */
  --spacing-md: 0.5rem;   /* 8px */
  --spacing-lg: 0.75rem;  /* 12px */
  --spacing-xl: 1rem;     /* 16px */
  --spacing-2xl: 1.25rem; /* 20px */
  --spacing-3xl: 1.5rem;  /* 24px */
  --spacing-4xl: 2rem;    /* 32px */
}

/* Custom animations */
@keyframes slideIn {
  from {
    transform: translateX(-100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes scaleIn {
  from {
    transform: scale(0.9);
    opacity: 0;
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}

.animate-slide-in {
  animation: slideIn 0.3s ease-out;
}

.animate-fade-in {
  animation: fadeIn 0.5s ease-out;
}

.animate-scale-in {
  animation: scaleIn 0.3s ease-out;
}

/* Brand component spacing utilities */
.brand-spacing-xs { gap: var(--spacing-xs); }
.brand-spacing-sm { gap: var(--spacing-sm); }
.brand-spacing-md { gap: var(--spacing-md); }
.brand-spacing-lg { gap: var(--spacing-lg); }
.brand-spacing-xl { gap: var(--spacing-xl); }
.brand-spacing-2xl { gap: var(--spacing-2xl); }
.brand-spacing-3xl { gap: var(--spacing-3xl); }

.brand-padding-xs { padding: var(--spacing-xs); }
.brand-padding-sm { padding: var(--spacing-sm); }
.brand-padding-md { padding: var(--spacing-md); }
.brand-padding-lg { padding: var(--spacing-lg); }
.brand-padding-xl { padding: var(--spacing-xl); }
.brand-padding-2xl { padding: var(--spacing-2xl); }
.brand-padding-3xl { padding: var(--spacing-3xl); }

.brand-margin-xs { margin: var(--spacing-xs); }
.brand-margin-sm { margin: var(--spacing-sm); }
.brand-margin-md { margin: var(--spacing-md); }
.brand-margin-lg { margin: var(--spacing-lg); }
.brand-margin-xl { margin: var(--spacing-xl); }
.brand-margin-2xl { margin: var(--spacing-2xl); }
.brand-margin-3xl { margin: var(--spacing-3xl); }

/* Hover and tap animations */
.btn-animate {
  transition: all 0.2s ease;
}

.btn-animate:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(168, 218, 220, 0.3);
}

.btn-animate:active {
  transform: translateY(0);
}

/* Custom gradient for cards */
.card-gradient {
  background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
}

/* Brand-consistent card spacing - Tighter layouts */
.brand-card {
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-md);
  border-radius: var(--radius-lg);
  border: 1px solid var(--border);
  background: var(--card);
}

.brand-card-compact {
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-sm);
  border-radius: var(--radius-md);
  border: 1px solid var(--border);
  background: var(--card);
}

.brand-card-header {
  margin-bottom: var(--spacing-md);
  padding-bottom: var(--spacing-sm);
  border-bottom: 1px solid var(--border);
}

.brand-card-content {
  margin-bottom: var(--spacing-md);
}

.brand-card-footer {
  margin-top: var(--spacing-md);
  padding-top: var(--spacing-sm);
  border-top: 1px solid var(--border);
}

/* Tight component spacing utilities */
.tight-spacing > * + * {
  margin-top: var(--spacing-sm);
}

.ultra-tight-spacing > * + * {
  margin-top: var(--spacing-xs);
}

.seamless-layout {
  gap: var(--spacing-xs);
}

.compact-layout {
  gap: var(--spacing-sm);
}

/* Header-content integration - Updated for taller header */
.header-content-tight {
  padding-top: var(--spacing-xl);
}

.header-content-seamless {
  padding-top: var(--spacing-lg);
}

.under-header {
  margin-top: calc(-1 * var(--spacing-md));
}

/* Enhanced header styling */
.enhanced-header {
  min-height: 4rem; /* Minimum height for the enhanced header */
}

.enhanced-header-content {
  padding-top: 2.5rem; /* Account for taller header */
}

/* Footer-content integration */
.content-footer-tight {
  padding-bottom: var(--spacing-2xl);
}

.content-footer-seamless {
  padding-bottom: var(--spacing-xl);
}

.above-footer {
  margin-bottom: calc(-1 * var(--spacing-sm));
}

/* Food image styles */
.food-image {
  transition: transform 0.3s ease;
  background-color: var(--muted);
  object-fit: cover;
}

.food-image:hover {
  transform: scale(1.05);
}

/* Image loading states */
img {
  display: block;
  max-width: 100%;
  height: auto;
}

img[src=""] {
  display: none;
}

/* Ensure proper image rendering */
.image-container {
  background-color: var(--muted);
  position: relative;
  overflow: hidden;
}

.image-container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, var(--pastel-yellow) 0%, var(--pastel-blue) 100%);
  opacity: 0.1;
  z-index: -1;
}

/* Enhanced scroll styles for CalorieCalculator with drag-aware behavior */
.hover-scroll-area {
  scrollbar-width: thin;
  scrollbar-color: var(--primary) transparent;
  scroll-behavior: smooth;
  overscroll-behavior: contain;
  transition: all 0.2s ease;
}

.hover-scroll-area::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

.hover-scroll-area::-webkit-scrollbar-track {
  background: rgba(0, 0, 0, 0.05);
  border-radius: 3px;
}

.hover-scroll-area::-webkit-scrollbar-thumb {
  background: var(--primary);
  border-radius: 3px;
  opacity: 0.4;
  transition: opacity 0.2s ease;
}

.hover-scroll-area::-webkit-scrollbar-thumb:hover {
  opacity: 0.8;
  background: var(--pastel-blue-dark);
}

.hover-scroll-area::-webkit-scrollbar-corner {
  background: transparent;
}

/* Show scrollbar on hover or focus */
.hover-scroll-container:hover .hover-scroll-area::-webkit-scrollbar-thumb,
.hover-scroll-area:focus-within::-webkit-scrollbar-thumb {
  opacity: 0.6;
}

/* Enhanced scroll item spacing with drag states */
.hover-scroll-item {
  scroll-margin: 8px;
  scroll-snap-align: start;
  transition: all 0.2s ease;
}

/* Scroll disabled state during drag */
.scroll-disabled {
  overflow: hidden !important;
  scroll-behavior: auto !important;
  touch-action: none !important;
  -webkit-overflow-scrolling: auto !important;
}

.scroll-disabled::-webkit-scrollbar-thumb {
  opacity: 0.2 !important;
}

/* Drag-aware scroll container states */
.ingredients-scrollable {
  touch-action: pan-y pinch-zoom;
  -webkit-overflow-scrolling: touch;
  overscroll-behavior: contain;
}

.ingredients-dragging {
  touch-action: none !important;
  user-select: none;
  -webkit-user-select: none;
  pointer-events: auto;
  overflow: hidden;
}

.ingredients-dragging * {
  pointer-events: none;
}

.ingredients-dragging .dragging-item {
  pointer-events: auto;
}

/* Dragging item state */
.dragging-item {
  position: relative;
  z-index: 1000 !important;
  box-shadow: 0 10px 25px rgba(168, 218, 220, 0.4) !important;
  transform: rotate(2deg) scale(1.05) !important;
  transition: none !important;
  pointer-events: auto !important;
  touch-action: none !important;
}

/* Improved touch handling for drag items */
.group.cursor-grab {
  cursor: grab;
  -webkit-user-drag: element;
}

.group.cursor-grab:active {
  cursor: grabbing;
}

/* Prevent scroll during potential drag */
.ingredients-scroll-area.touch-dragging {
  overflow: hidden !important;
  touch-action: none !important;
}

/* Touch target improvements */
.hover-scroll-item {
  min-height: 44px; /* Improved touch target size */
  position: relative;
}

/* Tap highlight removal for better UX */
.hover-scroll-item * {
  -webkit-tap-highlight-color: transparent;
  -webkit-touch-callout: none;
}

/* Auto-scroll enabled state during drag */
.drag-scroll-enabled {
  /* Allow main window scroll during drag */
  overflow: visible !important;
  touch-action: manipulation !important;
}

.drag-scroll-enabled * {
  /* Prevent child elements from interfering with drag */
  pointer-events: none;
}

.drag-scroll-enabled .dragging-item {
  /* Ensure dragged item remains interactive */
  pointer-events: auto !important;
}

.drag-scroll-enabled .group {
  /* Allow drag source to remain interactive */
  pointer-events: auto;
}

/* Auto-scroll zone styling */
.auto-scroll-zone {
  position: fixed;
  left: 0;
  right: 0;
  height: 80px;
  z-index: 40;
  pointer-events: none;
  transition: all 0.3s ease;
}

.auto-scroll-zone.active {
  background: linear-gradient(var(--primary)/20, transparent);
}

.auto-scroll-zone.top {
  top: 0;
  background: linear-gradient(to bottom, var(--primary)/10, transparent);
}

.auto-scroll-zone.bottom {
  bottom: 0;
  background: linear-gradient(to top, var(--primary)/10, transparent);
}

/* Scroll velocity indicator */
.scroll-velocity-indicator {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 50;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.2s ease;
}

.scroll-velocity-indicator.active {
  opacity: 1;
}

/* Enhanced drag feedback */
.drag-feedback-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 30;
  pointer-events: none;
  background: rgba(168, 218, 220, 0.05);
  backdrop-filter: blur(1px);
  transition: all 0.2s ease;
}

/* Improved dragging item with auto-scroll awareness */
.dragging-item-enhanced {
  position: fixed !important;
  z-index: 1000 !important;
  pointer-events: none !important;
  transform: rotate(2deg) scale(1.05) !important;
  box-shadow: 0 15px 35px rgba(168, 218, 220, 0.4) !important;
  border: 2px solid var(--primary) !important;
  background: white !important;
}

/* Auto-scroll animation */
@keyframes autoScrollPulse {
  0%, 100% { opacity: 0.6; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.05); }
}

.auto-scroll-active .auto-scroll-indicator {
  animation: autoScrollPulse 1s infinite;
}

/* Smooth scroll snap for mobile with drag considerations */
@media (max-width: 768px) {
  .hover-scroll-area {
    scroll-snap-type: y proximity;
    -webkit-overflow-scrolling: touch;
  }
  
  .hover-scroll-area.scroll-disabled {
    -webkit-overflow-scrolling: auto;
    scroll-snap-type: none;
  }
}

/* Ingredients library specific enhancements with drag states */
.ingredients-scroll-area {
  position: relative;
  isolation: isolate;
}

.ingredients-scroll-area::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 12px;
  background: linear-gradient(to bottom, var(--background) 60%, transparent);
  pointer-events: none;
  z-index: 2;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.ingredients-scroll-area::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 12px;
  background: linear-gradient(to top, var(--background) 60%, transparent);
  pointer-events: none;
  z-index: 2;
  opacity: 0;
  transition: opacity 0.3s ease;
}

/* Show fade gradients during scroll or hover */
.ingredients-scroll-area:hover::before,
.ingredients-scroll-area:hover::after,
.ingredients-scroll-area.scrolling::before,
.ingredients-scroll-area.scrolling::after {
  opacity: 1;
}

/* Hide gradients during drag */
.ingredients-dragging .ingredients-scroll-area::before,
.ingredients-dragging .ingredients-scroll-area::after {
  opacity: 0 !important;
}

/* Auto-scroll zones for drag operations */
.ingredients-scroll-area .scroll-zone-top,
.ingredients-scroll-area .scroll-zone-bottom {
  position: absolute;
  left: 0;
  right: 0;
  height: 40px;
  z-index: 3;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.2s ease;
}

.ingredients-scroll-area .scroll-zone-top {
  top: 0;
  background: linear-gradient(to bottom, var(--primary)/10, transparent);
}

.ingredients-scroll-area .scroll-zone-bottom {
  bottom: 0;
  background: linear-gradient(to top, var(--primary)/10, transparent);
}

/* Show scroll zones during drag near edges */
.ingredients-dragging .ingredients-scroll-area .scroll-zone-top:hover,
.ingredients-dragging .ingredients-scroll-area .scroll-zone-bottom:hover {
  opacity: 1;
}

/* Scroll indicator animation */
@keyframes scrollPulse {
  0%, 100% { opacity: 0.4; }
  50% { opacity: 0.8; }
}

.scroll-indicator {
  animation: scrollPulse 2s infinite;
}

/* Text truncation utilities */
.line-clamp-1 {
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.line-clamp-2 {
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.line-clamp-3 {
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* Enhanced food database list styling */
.food-database-list {
  scroll-behavior: smooth;
}

.food-database-item {
  transition: all 0.2s ease;
  border-left: 3px solid transparent;
}

.food-database-item:hover {
  border-left-color: var(--primary);
  background: linear-gradient(90deg, var(--primary)/5 0%, transparent 100%);
}